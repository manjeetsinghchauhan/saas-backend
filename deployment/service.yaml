apiVersion: v1
kind: Service
metadata:
  name: saas-backend
spec:
  type: ClusterIP  # Changed back to ClusterIP - Ingress controller handles external access
  selector:
    app: saas-backend
  ports:
    - name: http
      port: 80
      targetPort: 4000
      protocol: TCP

